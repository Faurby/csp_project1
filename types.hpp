#ifndef TYPES_HPP
#define TYPES_HPP

#include <vector>
#include <utility>
#include <cstdint>

// Type aliases
using Tuple = std::pair<uint64_t, uint64_t>;
using TupleVector = std::vector<Tuple>;

#endif